import { constants } from "../constants";

const initialState = {
  productList: null,
  slabList: null,
  pincodeList: null,
  notifications: [],
};

export function company(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
